apiVersion: v1
kind: Service
metadata:
    name: sayalimahabareauthenticationservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sayalimahabareauthenticationservice